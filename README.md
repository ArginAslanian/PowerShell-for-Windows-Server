# PowerShell Configs (Windows Server)

A small personal repo of PowerShell **configuration scripts** for Windows Server (eg: DHCP, DNS, and other server setup tasks).  
Not day-to-day commands — just repeatable config files I want to keep handy.
